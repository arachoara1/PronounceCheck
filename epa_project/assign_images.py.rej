diff a/epa_project/assign_images.py b/epa_project/assign_images.py	(rejected hunks)
@@ -21,6 +21,10 @@
     # 연속된 공백 처리
     title = re.sub(r'\s+', ' ', title).strip()
     
+    title = title.replace("(", "").replace(")", "")
+    title = title.replace(",", "")
+    title = title.replace(" ", "")
+    
     return title
 
 def assign_images():
